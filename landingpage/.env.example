# Where the landing page's navigation and CTA buttons send people.
# BUILD-TIME: Vite inlines these, so changing either needs a rebuild.
# In docker/Coolify, set them in the root .env instead.
VITE_CLIENT_URL=http://localhost:5173
VITE_BUSINESS_URL=http://localhost:3000
